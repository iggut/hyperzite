type: files
files:
  - source: system/usr
    destination: /usr
  - source: system/etc
    destination: /etc
  - source: home/config
    destination: ~/.config
  - source: home/config
    destination: $HOME/.config
